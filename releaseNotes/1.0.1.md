Initial release of the Postgres Plugin.

- Add functionality to pull from an external Postgres database
- Return either single value or full row based on Return Single Value input (`YES`, `NO`)
- Add functionality to INSERT data into an external Postgres database